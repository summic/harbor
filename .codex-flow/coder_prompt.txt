You are the coder.
Task:
Sail 服务质量体系一期（console 侧优先）：接入真实后端数据，完成 24h 稳定性视图、关键域名 TopN、失败原因标准化展示；补充相关测试；若仓库不存在 docs 目录请创建，并新增 docs/quality-observability.md。

Rules:
- Implement only what is necessary for this task.
- Keep changes focused and minimal.
- Add or update tests where practical.
- Do NOT commit.

Verifier feedback from previous attempt (if any):
- Numeric strings from the backend get coerced to 0 because `toNumber` only accepts `number` types; if the API sends `"99.2"` or `"12000"` as strings, success rates and counts will render as zero, collapsing charts and TopN ratios. Fix by parsing numeric strings or relaxing the guard in `utils/quality.ts:106-154`.
- Tests failed: `npm test` → `vitest: command not found` (per provided log), so the current test run is not green.

When done, print a short summary:
STATUS: done
SUMMARY:
- ...
